PORT=8000
RATE_LIMIT_PER_MINUTE=240
BACKEND_URL=http://localhost:5000

# Frontend server serves existing HTML/CSS/JS and proxies API calls to BACKEND_URL.
# Start the Mongo/Express backend from ./backend with npm run dev.
